apiVersion: apps/v1
kind: Deployment
metadata:
  name: food-delivery-backend
  namespace: food-delivery
  labels:
    app: food-delivery-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: food-delivery-backend
  template:
    metadata:
      labels:
        app: food-delivery-backend
        tags.datadoghq.com/env: "lab"
        tags.datadoghq.com/service: "food-delivery-backend"
        tags.datadoghq.com/version: "1.0.0"
      annotations:
        ad.datadoghq.com/food-delivery-backend.logs: '[{"source":"nodejs","service":"food-delivery-backend"}]'
    spec:
      containers:
        - name: food-delivery-backend
          image: ${ECR_REGISTRY}/sre-lab/food-delivery-backend:${IMAGE_TAG}
          ports:
            - containerPort: 4000
          envFrom:
            - configMapRef:
                name: food-delivery-backend-config
            - secretRef:
                name: food-delivery-db-credentials
          env:
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 15
