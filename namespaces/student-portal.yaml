apiVersion: v1
kind: Namespace
metadata:
  name: student-portal
  labels:
    app.kubernetes.io/part-of: sre-lab
    env: lab
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: student-portal-quota
  namespace: student-portal
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "20"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: student-portal-limits
  namespace: student-portal
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 250m
        memory: 256Mi
      max:
        cpu: "1"
        memory: 1Gi
      min:
        cpu: 50m
        memory: 64Mi
